import { CreateDirectiveDirective } from './create-directive.directive';

describe('CreateDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CreateDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
